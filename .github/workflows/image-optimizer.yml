# 文件路径: .github/workflows/image-optimizer.yml

name: '🤖 Optimize Bundle Images with ShortPixel'

on:
  push:
    branches:
      - main
    paths:
      - 'content/**/*.jpg'
      - 'content/**/*.png'
      - 'content/**/*.jpeg'
  
  schedule:
    - cron: '0 0 * * *'
    
  workflow_dispatch:

jobs:
  build:
    name: Convert Images Job
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      
    if: github.event.pusher.name != 'GitHub Actions Bot' && github.event.pusher.name != 'github-actions[bot]'

    steps:
      - name: '⬇️ Checkout Code'
        uses: actions/checkout@v4

      - name: '🔧 Install Dependencies'
        run: sudo apt-get update && sudo apt-get install -y imagemagick jq dnsutils

      - name: '🔄 Find Images to Process'
        id: find_files
        run: |
          echo "🔍 Finding images that need conversion..."
          UNCONVERTED_FILES=""
          # <<< FIX: Use Process Substitution to avoid subshell issues >>>
          # This ensures the UNCONVERTED_FILES variable is modified in the current shell.
          # The -print0 and -d '' combination robustly handles filenames with spaces.
          while IFS= read -r -d '' f; do
            [ ! -f "${f%.*}.avif" ] && UNCONVERTED_FILES="$UNCONVERTED_FILES$f "
          done < <(find content -type f $ -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" $ -print0)
          
          if [ -n "$UNCONVERTED_FILES" ]; then
            echo "  - Found $(echo $UNCONVERTED_FILES | wc -w) unconverted images."
            echo "files_to_process=${UNCONVERTED_FILES}" >> $GITHUB_OUTPUT
          else
            echo "✅ No images to process."
            echo "files_to_process=" >> $GITHUB_OUTPUT
          fi
          
      - name: '🚀 Process Images with ShortPixel and Smart Polling'
        if: steps.find_files.outputs.files_to_process != ''
        env:
          SHORTPIXEL_KEY: ${{ secrets.SHORTPIXEL_KEY }}
          FILES_TO_PROCESS: ${{ steps.find_files.outputs.files_to_process }}
          MAX_ATTEMPTS: 5
          POLL_INTERVAL_SECONDS: 60
        run: |
          PENDING_URLS_STRING=$(echo "$FILES_TO_PROCESS" | tr ' ' '\n' | jq -R . | jq -s 'map("https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/" + .)')
          
          for attempt in $(seq 1 $MAX_ATTEMPTS); do
            if [ "$(echo "$PENDING_URLS_STRING" | jq 'length')" -eq 0 ]; then
                echo "🎉 All images processed successfully!"
                break
            fi

            echo -e "\n-=================================-\n-   ATTEMPT ${attempt} / ${MAX_ATTEMPTS}\n-   Processing $(echo "$PENDING_URL_STRING" | jq 'length') images...\n-=================================-\n"

            JSON_PAYLOAD=$(jq -n --arg key "$SHORTPIXEL_KEY" --argjson urls "$PENDING_URLS_STRING" '{
              key: $key, plugin_version: "GHA42", lossy: 2, wait: 30, convertto: "+avif", urllist: $urls
            }')

            echo "📞 Calling ShortPixel API..."
            API_RESPONSE=$(curl -s -X POST -H "Content-Type: application/json" -d "$JSON_PAYLOAD" https://api.shortpixel.com/v2/reducer.php)
            
            NEXT_PENDING_URLS_STRING="[]"

            echo "✅ API call successful. Analyzing results..."
            echo "$API_RESPONSE" | jq -c '.[]' | while read -r item; do
              original_url=$(echo "$item" | jq -r .OriginalURL)
              original_path=$(echo "$original_url" | sed "s|https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/||")
              status_code=$(echo "$item" | jq -r .Status.Code)
              status_message=$(echo "$item" | jq -r .Status.Message)

              if [ "$status_code" == "2" ]; then
                  avif_url=$(echo "$item" | jq -r .AVIFURL)
                  if [ "$avif_url" == "null" ] || [ -z "$avif_url" ]; then echo "    - ⚠️ UNEXPECTED: Status 2 but AVIF URL is null for '$original_path'."; continue; fi
                  dir_path=$(dirname "$original_path"); base_name=$(basename "$original_path" | sed 's/\.[^.]*$//'); avif_path="$dir_path/$base_name.avif"
                  echo "    - ✅ SUCCESS ($status_code): Downloading for $original_path"
                  
                  temp_url="${avif_url#*//}"; url_hostname="${temp_url%%/*}"; ip_address=$(dig @1.1.1.1 "$url_hostname" +short | head -n 1)
                  if [ -z "$ip_address" ]; then echo "    -   ❌ DNS resolution failed for '${url_hostname}'. Will retry."; NEXT_PENDING_URLS_STRING=$(echo "$NEXT_PENDING_URLS_STRING" | jq --arg url "$original_url" '. + [$url]'); continue; fi
                  
                  curl -fsS --retry 3 --resolve "${url_hostname}:443:${ip_address}" -o "$avif_path" "$avif_url"
                  if [ -s "$avif_path" ]; then
                      dimensions=$(identify -format "%wx%h" "$original_path")
                      yaml_file="$dir_path/.image_dimensions.yaml"; touch "$yaml_file"
                      grep -q "${base_name}:" "$yaml_file" && sed -i "s|${base_name}:.*|${base_name}: ${dimensions}|" "$yaml_file" || echo "${base_name}: ${dimensions}" >> "$yaml_file"
                      echo "    -   Saved dimensions and removing original file: $original_path"
                      rm "$original_path"
                  else
                      echo "    -   ❌ Download failed for '$original_path'. Will retry."
                      NEXT_PENDING_URLS_STRING=$(echo "$NEXT_PENDING_URLS_STRING" | jq --arg url "$original_url" '. + [$url]')
                  fi
              elif [ "$status_code" == "1" ]; then
                  echo "    - ⏳ IN PROGRESS ($status_code): '$original_path' still processing. Will retry."
                  NEXT_PENDING_URLS_STRING=$(echo "$NEXT_PENDING_URLS_STRING" | jq --arg url "$original_url" '. + [$url]')
              else
                  echo
